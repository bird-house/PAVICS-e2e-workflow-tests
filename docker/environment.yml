# conda env create -f environment.yml
name: birdy
channels:
  - birdhouse
  - conda-forge
  - defaults
dependencies:
  - matplotlib
  - xarray
  - numpy
  - birdhouse-birdy
  - netcdf4
  - pydap
  - cartopy
  # tests
  - pytest
  - nbval
  # to edit .ipynb
  - jupyter
  # to diff .ipynb files
  - nbdime
